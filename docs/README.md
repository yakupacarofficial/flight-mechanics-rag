# Knowledge Base

English summary notes on flight-mechanics and aerodynamics, written in
my own words while studying an aviation-theory textbook. One topic per
file; `##` section headings split each file into chunks.

`ingest.py` reads every topic file here (this README is skipped),
vectorises the chunks with TF-IDF, and stores them in SQLite.
